type User {
    userName: String!,
    email: String!,
}

type CreateUserResult {
    result: User,
    errors: [String]!
}

type Mutation {
  "Returns an error array or an empty array"
  createUser(
      email: String!
      password: String!
      username: String
  ): CreateUserResult!
}

type Query {
  "gql file without query is not working :("
  dummy: String
}
